# 02 - 旧任务打开慢

## 现象

新任务秒开，旧任务要等数秒甚至更久；打开后带出大量子 Agent、网页、图片生成历史。

## 原因

旧任务恢复时需要读取并渲染完整事件流。大体积来源通常是：图片生成事件、工具输出、长对话。归档只移出活跃列表，不压缩文件。

## 处理

1. 找出大会话：按 rollout JSONL 大小排序（参考 scripts）。
2. 仍需继续的任务：新建精简交接任务，只带目标、进度、关键文件、待办。
3. 已完成任务：在应用内归档。
4. 不要无限延长超大会话。

## 判断

若归档后再次打开仍慢，说明会话文件本身大；应走交接方案而不是重复打开。
